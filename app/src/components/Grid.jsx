import React from 'react';

const Grid = props => {

  return (
    <div className='Grid' style={{'width' : `${props.gridSize}px`, 'height' : `${props.gridSize}px`}}>
      
    </div>
  )
}

export default Grid;